# Sprint 1 — Integración célula

Material de planificación, PoCs e informes por rol (referencia académica).

```
sprint-1/
├── backlog/          Sprint Planning + Excel backlog
├── informes/
│   ├── backend/      PoCs n8n, BD, LLMs, MapTiler, Phoenix
│   ├── frontend/     Flujo página, Figma, PoC respuesta
│   ├── arquitectura/ Informe integración IA
│   └── devsecops/    Manual Docker + PoC infra local
└── assets/           Diagramas arquitectura y flujos n8n
```

**Código ejecutable** vive en `src/` e `infrastructure/` (no solo en estos informes).

Despliegue: ver `docs/DEPLOY.md`.
